import React from 'react';
import { products } from './products';

const ProductsList = () => {
  return (
    <div>
      {products.map(() => (
        <p>adasdas</p>
      ))}
    </div>
  );
};

export default ProductsList;